node_modules
.svelte-kit
build
# .env is committed: it holds only the public API base URL (no secrets) and
# is required for the static build's $env/static/public import to resolve.
.env.local
.env.*.local

# Tauri
src-tauri/target
src-tauri/gen
