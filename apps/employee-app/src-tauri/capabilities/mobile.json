{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Mobile-only capabilities (secure token storage).",
  "platforms": ["iOS", "android"],
  "windows": ["main"],
  "permissions": ["stronghold:default"]
}
