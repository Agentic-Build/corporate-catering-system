name: OpenAPI Contract Platform

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  contract:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Install TypeScript generator dependencies
        run: pnpm install --frozen-lockfile

      - name: Contract conformance gate
        run: |
          cargo test --test openapi_contract_platform
          pnpm run contract:verify

      - name: Run Rust test suite
        run: cargo test --all-targets

      - name: Upload machine-readable spec artifact
        uses: actions/upload-artifact@v4
        with:
          name: openapi-spec
          path: |
            contract/openapi/openapi.json
            contract/openapi/openapi.yaml

      - name: Upload browsable docs artifact
        uses: actions/upload-artifact@v4
        with:
          name: openapi-docs
          path: |
            contract/openapi/index.html
            contract/openapi/openapi.json

      - name: Upload generated TypeScript client artifact
        uses: actions/upload-artifact@v4
        with:
          name: generated-ts-client
          path: contract/generated/ts-client
