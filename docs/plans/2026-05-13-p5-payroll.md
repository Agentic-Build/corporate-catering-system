# P5 Payroll 月結批次 + HR 匯出 + 爭議流程 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (or superpowers:subagent-driven-development) to implement this plan task-by-task.

**Goal**：交付月結對帳流程 — 福委會 lock 月份的所有 picked_up 訂單成 payroll batch、生成 HR CSV 上傳 MinIO/GCS、員工可開爭議、福委會審核退款（picked_up → refunded、回補 quota？否，已消費）。

**Architecture**：`payroll_batch` 是 immutable 一旦 locked；`payroll_entry` append-only via trigger；`payroll_dispute` 可流轉但歷史保留。Build 階段 SELECT picked_up + no_show orders 按 user_id 聚合；Lock 階段切換狀態並插入 entries（per user × per batch）+ outbox event。Worker 接收 `payroll.batch_locked.v1` → generate CSV → 上傳 → 發 `payroll.export_ready.v1`。

**Tech Stack**：沿用 + `encoding/csv` + S3 SDK 已在 P0 抽象層備好 (`platform/storage`)，本 phase 補實作（MinIO 端與 GCS 端都用 aws-sdk-go-v2 + S3-compat endpoint）。

**Branch**：`feat/p5-payroll`（已切）

**Scope boundary**：
- P5 **做**：月度 batch + entry generation、HR CSV (UTF-8 BOM, 中文支援)、dispute open/resolve、refund (picked_up → refunded)、admin UI + employee dispute UI、payroll-settler worker
- P5 **不**做**：vendor 月結對帳報表（留 P6）、自動退稅機制、銀行匯款整合

---

## Task 1：Migration — payroll schema

`migrations/000005_payroll.up.sql`:

```sql
CREATE TYPE payroll_batch_status AS ENUM ('draft', 'locked', 'exported', 'closed');
CREATE TYPE payroll_dispute_status AS ENUM ('open', 'resolved_refund', 'resolved_reject', 'cancelled');

CREATE TABLE payroll_batch (
  id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  period_start  DATE NOT NULL,
  period_end    DATE NOT NULL,
  status        payroll_batch_status NOT NULL DEFAULT 'draft',
  locked_at     TIMESTAMPTZ,
  locked_by     UUID REFERENCES "user"(id),
  exported_at   TIMESTAMPTZ,
  export_uri    TEXT,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  CHECK (period_start <= period_end)
);
CREATE UNIQUE INDEX payroll_batch_period_idx ON payroll_batch(period_start, period_end);
CREATE INDEX payroll_batch_status_idx ON payroll_batch(status);

CREATE TABLE payroll_entry (
  id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  batch_id      UUID NOT NULL REFERENCES payroll_batch(id) ON DELETE CASCADE,
  user_id       UUID NOT NULL REFERENCES "user"(id) ON DELETE RESTRICT,
  order_ids     UUID[] NOT NULL,
  amount_minor  BIGINT NOT NULL CHECK (amount_minor >= 0),
  refunded_minor BIGINT NOT NULL DEFAULT 0 CHECK (refunded_minor >= 0),
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX payroll_entry_batch_idx ON payroll_entry(batch_id);
CREATE INDEX payroll_entry_user_idx ON payroll_entry(user_id);

-- Append-only protection on entries (refunds are tracked via payroll_dispute + entry.refunded_minor UPDATE,
-- so we allow UPDATE here, but NOT DELETE. Simpler than full append-only.)
CREATE OR REPLACE FUNCTION payroll_entry_no_delete() RETURNS trigger AS $$
BEGIN
  RAISE EXCEPTION 'payroll_entry is not deletable (op=%)', TG_OP;
END $$ LANGUAGE plpgsql;
CREATE TRIGGER payroll_entry_no_delete_trg BEFORE DELETE ON payroll_entry
  FOR EACH ROW EXECUTE FUNCTION payroll_entry_no_delete();

CREATE TABLE payroll_dispute (
  id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  entry_id      UUID NOT NULL REFERENCES payroll_entry(id) ON DELETE RESTRICT,
  order_id      UUID NOT NULL REFERENCES "order"(id),
  opened_by     UUID NOT NULL REFERENCES "user"(id),
  reason        TEXT NOT NULL,
  status        payroll_dispute_status NOT NULL DEFAULT 'open',
  resolution    TEXT NOT NULL DEFAULT '',
  resolved_by   UUID REFERENCES "user"(id),
  resolved_at   TIMESTAMPTZ,
  refund_minor  BIGINT NOT NULL DEFAULT 0,
  created_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);
CREATE INDEX payroll_dispute_entry_idx ON payroll_dispute(entry_id);
CREATE INDEX payroll_dispute_status_idx ON payroll_dispute(status);
CREATE INDEX payroll_dispute_opened_by_idx ON payroll_dispute(opened_by);
```

Down: drop in reverse order + DROP TYPE.

**Commit**: `feat(db): payroll schema (batch / entry / dispute) with append-only entries`

---

## Task 2：Payroll domain + Postgres repos

`services/api/internal/payroll/{types,errors,repository}.go` + `postgres/{batch_repo,entry_repo,dispute_repo}.go` + tests.

Domain:
- `Batch{ID, PeriodStart, PeriodEnd, Status (draft|locked|exported|closed), LockedAt, LockedBy, ExportedAt, ExportURI, CreatedAt, UpdatedAt}`
- `Entry{ID, BatchID, UserID, OrderIDs []string, AmountMinor, RefundedMinor, CreatedAt}`
- `Dispute{ID, EntryID, OrderID, OpenedBy, Reason, Status, Resolution, ResolvedBy, ResolvedAt, RefundMinor}`

Repo methods (highlights):
- `BatchRepo`: `Create / GetByID / GetByPeriod / UpdateStatus / List / ListPendingExport / SetExportInfo`
- `EntryRepo`: `CreateTx / ListByBatch / GetByID / IncrementRefundedTx`
- `DisputeRepo`: `Create / GetByID / UpdateStatusTx / ListByStatus / ListByUser`

Errors: `ErrBatchNotFound`, `ErrEntryNotFound`, `ErrDisputeNotFound`, `ErrBatchLocked`, `ErrInvalidTransition`, `ErrBatchPeriodExists`

TDD per repo. Total ~12 tests.

**Commit**: `feat(payroll): domain + postgres repos (TDD with testcontainers)`

---

## Task 3：Payroll service — Build, Lock, OpenDispute, ResolveRefund

`services/api/internal/payroll/service.go` + tests (mock-based + integration for transactional flows).

Methods:

- `BuildDraft(ctx, periodStart, periodEnd)` — aggregates picked_up + no_show orders in period by user_id; creates `payroll_batch` (status=draft) + N `payroll_entry` rows in a tx
- `Lock(ctx, batchID, adminUserID)` — draft → locked; sets locked_at + locked_by; appends `payroll.batch_locked.v1` outbox event
- `OpenDispute(ctx, entryID, orderID, openedBy, reason)` — creates dispute; user must own the entry's user_id
- `ResolveDispute(ctx, disputeID, adminUserID, refundMinor, resolution, status: refund|reject)` — atomically:
  - For status=refund: `dispute.status = resolved_refund` + `entry.refunded_minor += refundMinor` + transition order(s) → refunded + outbox `payroll.dispute_resolved.v1` + audit
  - For status=reject: dispute.status = resolved_reject only
- `ListBatches`, `GetBatch(includeEntries)`, `ListDisputes(filter)`, `ListMyDisputes(userID)`

Tests:
- BuildDraft with seeded 3 users × 5 orders (mix of picked_up + no_show) → 3 entries with correct sums
- Lock locks status + appends outbox event
- OpenDispute happy + forbidden (not owner)
- ResolveDispute refund happy: entry.refunded incremented + order → refunded
- ResolveDispute reject: no order state change

**Commit**: `feat(payroll): service (Build/Lock/OpenDispute/ResolveRefund) with transactional integrity`

---

## Task 4：Payroll huma handlers

`services/api/internal/payroll/http/handlers.go`.

Admin endpoints:
| Method | Path | Op |
|---|---|---|
| POST | `/api/admin/payroll/batches` | createPayrollBatch (period_start/end) |
| GET | `/api/admin/payroll/batches` | listPayrollBatches |
| GET | `/api/admin/payroll/batches/{id}` | getPayrollBatch (with entries) |
| POST | `/api/admin/payroll/batches/{id}/lock` | lockPayrollBatch |
| GET | `/api/admin/payroll/disputes` | listPayrollDisputes (filter by status) |
| POST | `/api/admin/payroll/disputes/{id}/resolve` | resolvePayrollDispute |

Employee endpoints:
| Method | Path | Op |
|---|---|---|
| POST | `/api/employee/disputes` | openMyDispute (entry_id, order_id, reason) |
| GET | `/api/employee/disputes` | listMyDisputes |

Wire into main.go + contract-export.

**Commit**: `feat(payroll): huma handlers for admin batches + disputes and employee dispute submission`

---

## Task 5：Payroll-settler worker — generate HR CSV

`services/api/internal/payroll/settler/settler.go`:
- Subscribes to `payroll.batch_locked.v1` (durable consumer `payroll-settler`)
- For each event: load batch + entries → generate CSV (UTF-8 BOM + headers: `employee_id, primary_email, display_name, plant, department, amount_ntd, batch_period`)
- Upload to S3-compat storage (object key: `payroll/{batch_id}.csv`)
- Update batch: `SetExportInfo(exportURI, exportedAt)` + status → exported
- Publish `payroll.export_ready.v1`

Storage abstraction: 
- Add `services/api/internal/platform/storage/s3.go` (aws-sdk-go-v2 + custom endpoint for MinIO)
- Config: `S3_ENDPOINT`, `S3_BUCKET`, `S3_ACCESS_KEY_ID`, `S3_SECRET_ACCESS_KEY`

Worker entrypoint: `cmd/tbite --role=worker` already runs outbox-relay; extend to also run settler concurrently via errgroup.

Tests:
- Integration test with NATS testcontainer + MinIO testcontainer:
  - Seed locked batch + entries
  - Manually publish `payroll.batch_locked.v1`
  - Wait for settler to consume; assert: object exists in MinIO, batch.status=exported, batch.export_uri populated

`go get github.com/aws/aws-sdk-go-v2 github.com/aws/aws-sdk-go-v2/config github.com/aws/aws-sdk-go-v2/service/s3 github.com/testcontainers/testcontainers-go/modules/minio`

**Commit**: `feat(payroll): settler worker generates HR CSV → S3 → publishes export_ready`

---

## Task 6：Admin payroll UI

`apps/admin/src/routes/payroll/+page.{svelte,server.ts}` — list batches with status pills + "建立月份" button
`apps/admin/src/routes/payroll/new/+page.{svelte,server.ts}` — period_start/end form
`apps/admin/src/routes/payroll/[id]/+page.{svelte,server.ts}` — detail with lock button + entry list + export URL when ready
`apps/admin/src/routes/payroll/[id]/disputes/+page.{svelte,server.ts}` — dispute list with resolve forms

Auto-refresh batch detail every 10s while in `locked` status (waiting for settler).

**Commit**: `feat(admin): payroll batch management + disputes UI`

---

## Task 7：Employee dispute UI

Extend `apps/employee/src/routes/orders/[id]/+page.svelte` to show 「申訴」 button when order has a non-disputed payroll_entry.

`apps/employee/src/routes/disputes/+page.{svelte,server.ts}` — list my open + resolved disputes
`apps/employee/src/routes/orders/[id]/dispute/+page.{svelte,server.ts}` — form with reason text + entry_id lookup

For P5 simplicity: only one dispute per order. Backend enforces via UNIQUE constraint or service check.

**Commit**: `feat(employee): dispute submission + list UI`

---

## Task 8：OpenAPI regen + e2e + docs + PR

```bash
make contract-sync
```

E2E spec (`tests/e2e/payroll-cycle.spec.ts`): admin creates a batch, locks it, sees status update (no actual settler run needed since worker is separate; verify lock succeeds + UI updates).

Update README + design doc §15. Push + PR.

**Commit**: `docs: mark P5 done; e2e payroll-cycle spec`

PR title: `P5: payroll batches + HR CSV export + dispute / refund flow`

---

## Exit Criteria

- [ ] Migrations up/down/up clean
- [ ] payroll repos + service tests pass (~15 tests)
- [ ] Settler worker integration test passes (event → CSV → S3 → status update)
- [ ] Admin UI loads + lock button works
- [ ] Employee dispute submission works
- [ ] OpenAPI drift gate green
- [ ] PR opened
