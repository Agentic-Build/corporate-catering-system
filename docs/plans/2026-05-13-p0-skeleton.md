# P0 Skeleton 實作計畫

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal**：建立全新 monorepo 骨架（pnpm workspace + Go module + 雙 K8s overlay），讓 `make dev-up` 起本機環境、三個 SvelteKit app dev server 渲染 hello world、Go API `/healthz` 回 200。

**Architecture**：依循 [`docs/plans/2026-05-13-tbite-refactor-design.md`](./2026-05-13-tbite-refactor-design.md) 第 3、4、8 章。先砍掉現有 Rust + 單一 SPA codebase（保留 INITIAL.md、ops/observability、docs），再分層建立 packages → apps → services/api → ops → CI。

**Tech Stack**：pnpm 9 + SvelteKit 2 (adapter-node) + Svelte 5 + Tailwind 3 + Go 1.23 + chi + sqlx-style migrations via `golang-migrate` + kustomize + k3d (single-node) / GKE (gcp) + GitHub Actions。

**測試策略**：P0 是骨架，幾乎沒有業務邏輯。每個 task 的「測試」是 **可執行的驗證指令** —— compile / lint / render / curl health。後續 P1+ 進入 TDD 流。

**Branch**：`feat/p0-skeleton`（已切換）

---

## Task 0：盤點現況與規劃刪除清單

**Files**:
- Read-only：`ls`, `find`

**Step 1**：列出將被刪除的 Rust + 舊前端檔案，產出明細到 `docs/plans/2026-05-13-p0-deletion-manifest.md`（暫存清單，P0 結束時刪除此檔）。

清單需包含：
- `Cargo.toml`、`Cargo.lock`、`src/**`、`tests/**`、`.sqlx/**`
- `apps/web/**`（舊單一 SPA）
- `Dockerfile.system`、`Dockerfile.web`、舊 `docker-compose.yml`
- `package.json`（root，將被新 pnpm workspace root 取代）
- `pnpm-lock.yaml`、`tsconfig.contract.json`、`contract-client-compile-check.ts`
- `target/`、`node_modules/`（用 .gitignore 排除即可，不在 git tracking）
- 舊 migrations (將完全重寫)
- 舊 `Makefile`

**保留**：
- `INITIAL.md`（需求源）
- `README.md`（後續會改寫）
- `docs/**`（含這份 plan）
- `ops/observability/**`（OTel/Victoria stack 沿用）
- `ops/kubernetes/base/**` 的 networkpolicy、pgbouncer topology 部分（後續 task 個別 review，先不刪）
- `.github/**`（後續 task 改寫 workflow）
- `.gitignore`、`.gitattributes`、`.dockerignore`
- 設計系統素材 → P0 不複製，packages/ui 的素材移植放在後續 task

**Step 2**：commit 清單

```bash
git add docs/plans/2026-05-13-p0-deletion-manifest.md
git commit -m "docs(plan): record P0 deletion manifest"
```

---

## Task 1：清除舊 Rust + 舊 web codebase

**Files**:
- Delete：依 Task 0 manifest

**Step 1**：刪除檔案

```bash
git rm -rf Cargo.toml Cargo.lock src tests .sqlx
git rm -rf apps/web
git rm -f Dockerfile.system Dockerfile.web docker-compose.yml
git rm -f package.json pnpm-lock.yaml tsconfig.contract.json contract-client-compile-check.ts
git rm -rf migrations
git rm -f Makefile
git rm -rf contract              # 舊 OpenAPI 工件，由 Go 重新生成
git rm -rf scripts               # 舊 scripts，後續 task 重寫
```

不在 git tracking 的 `target/`、`node_modules/` 用 OS 刪：

```bash
rm -rf target node_modules
```

**Step 2**：驗證

```bash
git status
```

期望輸出：大量 `deleted:`，無未追蹤殘留檔。

**Step 3**：commit

```bash
git commit -m "chore: remove legacy Rust backend and single-page SvelteKit web"
```

---

## Task 2：建立 pnpm workspace 與根目錄設定

**Files**:
- Create：`package.json`、`pnpm-workspace.yaml`、`.nvmrc`、`.npmrc`、`tsconfig.base.json`、`.editorconfig`、`.prettierrc`、`.prettierignore`

**Step 1**：`pnpm-workspace.yaml`

```yaml
packages:
  - "apps/*"
  - "packages/*"
```

**Step 2**：根 `package.json`

```json
{
  "name": "@tbite/root",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.12.0",
  "engines": { "node": ">=20.11" },
  "scripts": {
    "dev": "pnpm -r --parallel --filter \"./apps/*\" dev",
    "build": "pnpm -r --filter \"./packages/*\" build && pnpm -r --filter \"./apps/*\" build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "check": "pnpm -r check"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-svelte": "^3.2.7",
    "typescript": "^5.6.0"
  }
}
```

**Step 3**：`.nvmrc` → `20.18.0`、`.npmrc` → `engine-strict=true\nresolution-mode=highest\n`

**Step 4**：`tsconfig.base.json`

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "isolatedModules": true,
    "resolveJsonModule": true,
    "verbatimModuleSyntax": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  }
}
```

**Step 5**：`.editorconfig`、`.prettierrc`、`.prettierignore`（標準設定）

**Step 6**：驗證 + commit

```bash
pnpm install
git add package.json pnpm-workspace.yaml pnpm-lock.yaml .nvmrc .npmrc tsconfig.base.json .editorconfig .prettierrc .prettierignore
git commit -m "chore: bootstrap pnpm workspace and root tooling"
```

---

## Task 3：`packages/tokens` — 設計 token 與 Tailwind preset

**Files**:
- Create：
  - `packages/tokens/package.json`
  - `packages/tokens/src/tokens.css`
  - `packages/tokens/src/fonts.css`
  - `packages/tokens/src/tailwind-preset.cjs`
  - `packages/tokens/README.md`
  - `packages/tokens/index.ts`

**Step 1**：`packages/tokens/package.json`

```json
{
  "name": "@tbite/tokens",
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./index.ts",
    "./tokens.css": "./src/tokens.css",
    "./fonts.css": "./src/fonts.css",
    "./tailwind": "./src/tailwind-preset.cjs"
  },
  "files": ["src", "index.ts"],
  "scripts": { "check": "tsc --noEmit -p tsconfig.json" },
  "devDependencies": { "typescript": "^5.6.0" }
}
```

**Step 2**：把 `~/Downloads/T-Bite Design System/colors_and_type.css` 複製到 `packages/tokens/src/tokens.css`

```bash
cp "$HOME/Downloads/T-Bite Design System/colors_and_type.css" packages/tokens/src/tokens.css
```

**Step 3**：`packages/tokens/src/fonts.css` —— 透過 Google Fonts 引入（MVP 簡化，後續 task 改自託管）

```css
@import url("https://fonts.googleapis.com/css2?family=Noto+Sans+TC:wght@400;500;600;700;800;900&family=JetBrains+Mono:wght@500;600&display=swap");
```

**Step 4**：`packages/tokens/src/tailwind-preset.cjs` —— 把 colors_and_type.css 的關鍵 token 映射成 Tailwind theme

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      colors: {
        "tb-red": {
          50: "#fef2f2",
          500: "#ef4444",
          600: "#dc2626",
          700: "#b91c1c",
        },
        "tb-rose": {
          50: "#fff1f2",
          600: "#e11d48",
          700: "#be123c",
        },
        "tb-amber": {
          50: "#fffbeb",
          300: "#fcd34d",
          400: "#fbbf24",
        },
        "tb-slate": {
          50: "#f8fafc",
          100: "#f1f5f9",
          200: "#e2e8f0",
          300: "#cbd5e1",
          500: "#64748b",
          800: "#1e293b",
          900: "#0f172a",
          950: "#020617",
        },
        "tb-emerald": { 500: "#10b981" },
      },
      fontFamily: {
        "noto-tc": [
          '"Noto Sans TC"',
          '"PingFang TC"',
          '"Microsoft JhengHei"',
          "system-ui",
          "sans-serif",
        ],
        "jetbrains-mono": ['"JetBrains Mono"', "ui-monospace", "monospace"],
      },
      borderRadius: { "tb-2xl": "16px" },
      boxShadow: {
        "tb-sm": "0 1px 2px 0 rgba(15, 23, 42, 0.06)",
        "tb-md": "0 4px 6px -1px rgba(15, 23, 42, 0.10)",
      },
      letterSpacing: { eyebrow: "0.18em", "eyebrow-wide": "0.22em" },
      keyframes: {
        "tb-fade-up": {
          "0%": { opacity: "0", transform: "translateY(8px)" },
          "100%": { opacity: "1", transform: "translateY(0)" },
        },
        "tb-cart-bump": {
          "0%,100%": { transform: "scale(1)" },
          "50%": { transform: "scale(1.18)" },
        },
      },
      animation: {
        "tb-fade-up": "tb-fade-up 220ms cubic-bezier(.2,0,.2,1) both",
        "tb-cart-bump": "tb-cart-bump 320ms cubic-bezier(.2,0,.2,1)",
      },
    },
  },
};
```

**Step 5**：`packages/tokens/index.ts` —— 簡單 marker（讓 type 解析）

```ts
export const tokensVersion = "0.0.0";
```

**Step 6**：`tsconfig.json` extend `../../tsconfig.base.json`

**Step 7**：驗證

```bash
pnpm --filter @tbite/tokens check
```

期望：無錯誤輸出。

**Step 8**：commit

```bash
git add packages/tokens
git commit -m "feat(tokens): add T-Bite design tokens and Tailwind preset"
```

---

## Task 4：`packages/ui` 骨架（一個 demo Svelte 元件）

**Files**:
- Create：
  - `packages/ui/package.json`
  - `packages/ui/svelte.config.js`
  - `packages/ui/vite.config.ts`
  - `packages/ui/tsconfig.json`
  - `packages/ui/src/index.ts`
  - `packages/ui/src/Button.svelte`
  - `packages/ui/src/TBiteLogo.svelte`

**範圍說明**：P0 只放 **Button + TBiteLogo** 兩個元件作為三 app 共用驗證；其他元件移植排在 P1+。

**Step 1**：`packages/ui/package.json`

```json
{
  "name": "@tbite/ui",
  "version": "0.0.0",
  "type": "module",
  "svelte": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": { "svelte": "./src/index.ts", "types": "./src/index.ts" } },
  "files": ["src"],
  "peerDependencies": { "svelte": "^5.0.0" },
  "devDependencies": {
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0"
  },
  "scripts": { "check": "svelte-check --tsconfig ./tsconfig.json" }
}
```

**Step 2**：`packages/ui/src/Button.svelte`

```svelte
<script lang="ts">
  interface Props {
    variant?: "primary" | "secondary" | "ghost" | "danger";
    size?: "sm" | "md";
    type?: "button" | "submit";
    disabled?: boolean;
    onclick?: (e: MouseEvent) => void;
    children?: import("svelte").Snippet;
  }
  let {
    variant = "primary",
    size = "md",
    type = "button",
    disabled = false,
    onclick,
    children,
  }: Props = $props();

  const base =
    "inline-flex items-center justify-center rounded-lg border font-semibold transition focus:outline-none focus:ring-2 focus:ring-offset-1";
  const sizes = {
    sm: "text-xs px-2.5 py-1.5",
    md: "text-sm px-3.5 py-2",
  };
  const variants = {
    primary:
      "bg-tb-red-600 border-tb-red-600 text-white hover:bg-tb-red-700 focus:ring-tb-red-500",
    secondary:
      "bg-white border-tb-slate-300 text-tb-slate-800 hover:border-tb-slate-500 hover:text-tb-slate-950 focus:ring-tb-slate-300",
    ghost: "border-transparent text-tb-slate-800 hover:bg-tb-slate-100",
    danger:
      "bg-tb-rose-600 border-tb-rose-600 text-white hover:bg-tb-rose-700 focus:ring-tb-rose-500",
  };
</script>

<button
  {type}
  {disabled}
  class="{base} {sizes[size]} {variants[variant]} disabled:cursor-not-allowed disabled:opacity-60"
  {onclick}
>
  {@render children?.()}
</button>
```

**Step 3**：`packages/ui/src/TBiteLogo.svelte`（簡化版，從 reference `TBiteLogo.jsx` 移植）

```svelte
<script lang="ts">
  interface Props { size?: number; eyebrow?: boolean }
  let { size = 28, eyebrow = true }: Props = $props();
</script>

<a href="/" class="inline-flex items-center gap-2 font-noto-tc">
  <span
    class="relative inline-flex items-center justify-center rounded-full bg-gradient-to-br from-tb-red-500 to-tb-rose-700 text-white shadow-tb-sm"
    style:width="{size}px"
    style:height="{size}px"
  >
    <span class="text-[14px] font-black leading-none">T</span>
    <span
      class="absolute -right-0.5 -top-0.5 h-2 w-2 rounded-full bg-tb-amber-400"
      aria-hidden
    ></span>
  </span>
  <span class="flex flex-col leading-tight">
    <span class="text-[15px] font-black text-tb-slate-900">T-Bite.</span>
    {#if eyebrow}
      <span
        class="text-[9px] uppercase tracking-eyebrow-wide text-tb-slate-500"
        >Corporate Catering</span
      >
    {/if}
  </span>
</a>
```

**Step 4**：`packages/ui/src/index.ts`

```ts
export { default as Button } from "./Button.svelte";
export { default as TBiteLogo } from "./TBiteLogo.svelte";
```

**Step 5**：tsconfig + svelte.config.js（標準）

**Step 6**：驗證

```bash
pnpm install
pnpm --filter @tbite/ui check
```

期望：`svelte-check found 0 errors`

**Step 7**：commit

```bash
git add packages/ui
git commit -m "feat(ui): scaffold @tbite/ui with Button and TBiteLogo"
```

---

## Task 5：`packages/api-client` 預留骨架

**Files**:
- Create：`packages/api-client/package.json`、`packages/api-client/README.md`、`packages/api-client/src/index.ts`（暫時 export placeholder type）

**Step 1**：

```json
{
  "name": "@tbite/api-client",
  "version": "0.0.0",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "files": ["src"],
  "scripts": { "check": "tsc --noEmit -p tsconfig.json" },
  "devDependencies": { "typescript": "^5.6.0" }
}
```

`src/index.ts`：

```ts
export const apiClientVersion = "0.0.0";
// Generated client will replace this in P1 (contract:sync).
```

`README.md`：說明此 package 在 P1 由 `make contract-sync` 自動生成填入。

**Step 2**：驗證 + commit

```bash
pnpm --filter @tbite/api-client check
git add packages/api-client
git commit -m "feat(api-client): scaffold placeholder package for P1 generation"
```

---

## Task 6：`apps/employee` — 第一個 SvelteKit app

**Files**：完整 SvelteKit 2 + adapter-node + Svelte 5 + Tailwind 3 專案。關鍵檔：
- `apps/employee/package.json`
- `apps/employee/svelte.config.js`（adapter-node）
- `apps/employee/vite.config.ts`
- `apps/employee/tsconfig.json`
- `apps/employee/tailwind.config.cjs`
- `apps/employee/postcss.config.cjs`
- `apps/employee/src/app.html`
- `apps/employee/src/app.css`（@tailwind + import tokens）
- `apps/employee/src/lib/server/env.ts`
- `apps/employee/src/routes/+layout.svelte`
- `apps/employee/src/routes/+page.svelte`
- `apps/employee/src/routes/+page.server.ts`（demo SSR + fetch `/healthz` from Go API）
- `apps/employee/Dockerfile`

**Step 1**：`apps/employee/package.json`

```json
{
  "name": "@tbite/employee",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite dev --port 5173",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "lint": "prettier --check ."
  },
  "dependencies": {
    "@sveltejs/adapter-node": "^5.2.0",
    "@tbite/api-client": "workspace:*",
    "@tbite/tokens": "workspace:*",
    "@tbite/ui": "workspace:*"
  },
  "devDependencies": {
    "@sveltejs/kit": "^2.7.0",
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "tailwindcss": "^3.4.13",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
```

**Step 2**：`svelte.config.js`

```js
import adapter from "@sveltejs/adapter-node";
import { vitePreprocess } from "@sveltejs/vite-plugin-svelte";

export default {
  preprocess: vitePreprocess(),
  kit: { adapter: adapter({ out: "build", precompress: true }) },
};
```

**Step 3**：`tailwind.config.cjs`

```js
const preset = require("@tbite/tokens/tailwind");
module.exports = {
  presets: [preset],
  content: [
    "./src/**/*.{html,svelte,ts}",
    "../../packages/ui/src/**/*.{svelte,ts}",
  ],
};
```

**Step 4**：`src/app.html`

```html
<!doctype html>
<html lang="zh-Hant">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>T-Bite · Employee</title>
    %sveltekit.head%
  </head>
  <body data-sveltekit-preload-data="hover" class="bg-tb-slate-50 font-noto-tc">
    <div style="display: contents">%sveltekit.body%</div>
  </body>
</html>
```

**Step 5**：`src/app.css`

```css
@import "@tbite/tokens/fonts.css";
@import "@tbite/tokens/tokens.css";
@tailwind base;
@tailwind components;
@tailwind utilities;
```

**Step 6**：`src/lib/server/env.ts`

```ts
import { env } from "$env/dynamic/private";
export const API_BASE_URL = env.API_BASE_URL ?? "http://localhost:8080";
```

**Step 7**：`src/routes/+layout.svelte`

```svelte
<script lang="ts">
  import "../app.css";
  import { TBiteLogo } from "@tbite/ui";
  let { children } = $props();
</script>

<header
  class="sticky top-0 z-40 bg-white/95 px-6 py-4 shadow-tb-sm backdrop-blur"
>
  <TBiteLogo />
</header>
<main class="mx-auto max-w-[1400px] px-4 py-8 md:px-8">{@render children()}</main>
```

**Step 8**：`src/routes/+page.server.ts`

```ts
import { API_BASE_URL } from "$lib/server/env";
import type { PageServerLoad } from "./$types";

export const load: PageServerLoad = async ({ fetch }) => {
  let apiHealth: "ok" | "down" = "down";
  try {
    const res = await fetch(`${API_BASE_URL}/healthz`);
    if (res.ok) apiHealth = "ok";
  } catch {}
  return { apiHealth };
};
```

**Step 9**：`src/routes/+page.svelte`

```svelte
<script lang="ts">
  import { Button } from "@tbite/ui";
  let { data } = $props();
</script>

<h1 class="text-3xl font-black text-tb-slate-900">哈囉，雅筑 👋</h1>
<p class="mt-2 text-tb-slate-500">P0 skeleton · employee app</p>
<p class="mt-1 text-sm">
  API health:
  <span
    class="font-jetbrains-mono"
    class:text-tb-emerald-500={data.apiHealth === "ok"}
    class:text-tb-rose-600={data.apiHealth !== "ok"}
  >
    {data.apiHealth}
  </span>
</p>
<div class="mt-6"><Button>送出預訂 · 由本月薪資代扣</Button></div>
```

**Step 10**：`Dockerfile`（multi-stage）

```dockerfile
FROM node:20-alpine AS builder
WORKDIR /repo
RUN corepack enable
COPY pnpm-workspace.yaml package.json pnpm-lock.yaml ./
COPY apps/employee ./apps/employee
COPY packages ./packages
RUN pnpm install --frozen-lockfile
RUN pnpm --filter @tbite/employee build

FROM node:20-alpine AS runner
WORKDIR /app
COPY --from=builder /repo/apps/employee/build ./
COPY --from=builder /repo/apps/employee/package.json ./
ENV NODE_ENV=production
EXPOSE 3000
CMD ["node", "index.js"]
```

**Step 11**：驗證

```bash
pnpm install
pnpm --filter @tbite/employee check
pnpm --filter @tbite/employee build
```

期望：build 成功、`apps/employee/build/index.js` 存在。

**Step 12**：commit

```bash
git add apps/employee
git commit -m "feat(employee): scaffold SvelteKit app with adapter-node"
```

---

## Task 7：`apps/merchant` 與 `apps/admin` 鏡像 Task 6

**Step 1**：腳本化建立兩 app（差異只在 name / port / title / 起手頁 H1）

- `apps/merchant`：port 5174，title `T-Bite · Merchant`，H1 「商家後台」，提示文字「P0 skeleton · merchant app」
- `apps/admin`：port 5175，title `T-Bite · Admin`，H1 「福委會治理」，提示文字「P0 skeleton · admin app」

**Step 2**：驗證

```bash
pnpm --filter @tbite/merchant check
pnpm --filter @tbite/admin check
pnpm --filter @tbite/merchant build
pnpm --filter @tbite/admin build
```

**Step 3**：commit

```bash
git add apps/merchant apps/admin
git commit -m "feat(web): scaffold merchant and admin SvelteKit apps"
```

---

## Task 8：Go module 骨架 + 三 entrypoint binary

**Files**:
- Create：
  - `go.mod`、`go.sum`
  - `services/api/cmd/tbite/main.go`
  - `services/api/internal/config/config.go`
  - `services/api/internal/observability/observability.go`（最小 logger）
  - `services/api/internal/httpserver/server.go`
  - `services/api/internal/httpserver/health.go`
  - `services/api/Dockerfile`
  - `services/api/.golangci.yaml`
  - `services/api/Makefile`（給 services/api 本地用，根目錄 Makefile 後續 task 建立）

**Step 1**：`go.mod`（最小依賴）

```go
module github.com/takalawang/corporate-catering-system/services/api

go 1.23

require (
    github.com/go-chi/chi/v5 v5.1.0
    github.com/spf13/pflag v1.0.5
    golang.org/x/sync v0.8.0
)
```

**Step 2**：`internal/config/config.go`

```go
package config

import (
    "fmt"
    "os"
    "strconv"
)

type Role string

const (
    RoleAPI       Role = "api"
    RoleWorker    Role = "worker"
    RoleScheduler Role = "scheduler"
)

type Config struct {
    Role         Role
    HTTPAddr     string
    LogLevel     string
}

func FromEnv() (Config, error) {
    c := Config{
        HTTPAddr: getenv("HTTP_ADDR", ":8080"),
        LogLevel: getenv("LOG_LEVEL", "info"),
    }
    return c, nil
}

func getenv(k, def string) string {
    v := os.Getenv(k)
    if v == "" {
        return def
    }
    return v
}

func ParseRole(s string) (Role, error) {
    switch Role(s) {
    case RoleAPI, RoleWorker, RoleScheduler:
        return Role(s), nil
    default:
        return "", fmt.Errorf("invalid role %q (want api|worker|scheduler)", s)
    }
}

func MustParsePort(s string) int {
    n, err := strconv.Atoi(s)
    if err != nil {
        panic(err)
    }
    return n
}
```

**Step 3**：`internal/httpserver/health.go`

```go
package httpserver

import (
    "encoding/json"
    "net/http"
)

func healthHandler(w http.ResponseWriter, r *http.Request) {
    w.Header().Set("content-type", "application/json")
    _ = json.NewEncoder(w).Encode(map[string]string{"status": "ok"})
}

func readyHandler(w http.ResponseWriter, r *http.Request) {
    w.Header().Set("content-type", "application/json")
    _ = json.NewEncoder(w).Encode(map[string]string{"status": "ready"})
}
```

**Step 4**：`internal/httpserver/server.go`

```go
package httpserver

import (
    "context"
    "log/slog"
    "net/http"
    "time"

    "github.com/go-chi/chi/v5"
    "github.com/go-chi/chi/v5/middleware"
)

type Server struct {
    addr   string
    logger *slog.Logger
    srv    *http.Server
}

func New(addr string, logger *slog.Logger) *Server {
    r := chi.NewRouter()
    r.Use(middleware.RequestID)
    r.Use(middleware.RealIP)
    r.Use(middleware.Recoverer)
    r.Use(middleware.Timeout(30 * time.Second))

    r.Get("/healthz", healthHandler)
    r.Get("/readyz", readyHandler)
    r.NotFound(func(w http.ResponseWriter, _ *http.Request) {
        http.Error(w, `{"error":"not_found"}`, http.StatusNotFound)
    })

    srv := &http.Server{
        Addr:              addr,
        Handler:           r,
        ReadHeaderTimeout: 5 * time.Second,
    }
    return &Server{addr: addr, logger: logger, srv: srv}
}

func (s *Server) Run(ctx context.Context) error {
    errCh := make(chan error, 1)
    go func() {
        s.logger.Info("http listening", "addr", s.addr)
        if err := s.srv.ListenAndServe(); err != nil && err != http.ErrServerClosed {
            errCh <- err
        }
    }()
    select {
    case <-ctx.Done():
        shutdownCtx, cancel := context.WithTimeout(context.Background(), 10*time.Second)
        defer cancel()
        return s.srv.Shutdown(shutdownCtx)
    case err := <-errCh:
        return err
    }
}
```

**Step 5**：`cmd/tbite/main.go`

```go
package main

import (
    "context"
    "fmt"
    "log/slog"
    "os"
    "os/signal"
    "syscall"

    "github.com/spf13/pflag"

    "github.com/takalawang/corporate-catering-system/services/api/internal/config"
    "github.com/takalawang/corporate-catering-system/services/api/internal/httpserver"
)

func main() {
    var roleStr string
    pflag.StringVar(&roleStr, "role", "api", "binary role: api|worker|scheduler")
    pflag.Parse()

    role, err := config.ParseRole(roleStr)
    if err != nil {
        fmt.Fprintln(os.Stderr, err)
        os.Exit(2)
    }

    cfg, err := config.FromEnv()
    if err != nil {
        fmt.Fprintln(os.Stderr, err)
        os.Exit(2)
    }
    cfg.Role = role

    logger := slog.New(slog.NewJSONHandler(os.Stdout, &slog.HandlerOptions{Level: slog.LevelInfo}))
    logger = logger.With("role", string(role))

    ctx, cancel := signal.NotifyContext(context.Background(), syscall.SIGINT, syscall.SIGTERM)
    defer cancel()

    switch role {
    case config.RoleAPI:
        srv := httpserver.New(cfg.HTTPAddr, logger)
        if err := srv.Run(ctx); err != nil {
            logger.Error("api shutdown", "err", err)
            os.Exit(1)
        }
    case config.RoleWorker:
        logger.Info("worker placeholder, exiting cleanly (P0)")
        <-ctx.Done()
    case config.RoleScheduler:
        logger.Info("scheduler placeholder, exiting cleanly (P0)")
        <-ctx.Done()
    }
}
```

**Step 6**：`Dockerfile`

```dockerfile
FROM golang:1.23-alpine AS builder
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -ldflags="-s -w" -o /out/tbite ./cmd/tbite

FROM gcr.io/distroless/static:nonroot
COPY --from=builder /out/tbite /usr/local/bin/tbite
USER nonroot:nonroot
ENTRYPOINT ["/usr/local/bin/tbite"]
CMD ["--role=api"]
```

**Step 7**：`.golangci.yaml`（標準 strict 設定）—— 略

**Step 8**：驗證

```bash
cd services/api
go build ./...
go test ./...
go vet ./...
./...# 本地跑：
./_/tbite --role=api &      # 透過 go run 替代
go run ./cmd/tbite --role=api &
sleep 1
curl -fsS http://localhost:8080/healthz
# 期望：{"status":"ok"}
pkill -f "tbite --role"
```

**Step 9**：commit

```bash
git add services/api go.mod go.sum
git commit -m "feat(api): scaffold Go modular monolith with multi-role entrypoint"
```

---

## Task 9：`golang-migrate` 設定 + 第一個空 migration

**Files**:
- Create：
  - `migrations/000001_init.up.sql`（空 SQL 或一句註解）
  - `migrations/000001_init.down.sql`
  - `services/api/internal/persistence/migrator.go`（embed + 啟動時 migrate up 的 hook，先預留 interface 不啟用）
  - `scripts/db/migrate.sh`（CLI 包裝）

**Step 1**：`migrations/000001_init.up.sql`

```sql
-- P0 placeholder migration. Real schema lands in P2.
SELECT 1;
```

`migrations/000001_init.down.sql`

```sql
SELECT 1;
```

**Step 2**：`scripts/db/migrate.sh`

```bash
#!/usr/bin/env bash
set -euo pipefail
DB_URL="${DATABASE_URL:-postgres://tbite:tbite@localhost:5432/tbite?sslmode=disable}"
docker run --rm --network host -v "$(pwd)/migrations:/migrations" \
  migrate/migrate:v4.18.1 -path=/migrations -database "$DB_URL" "$@"
```

讓 `make migrate-up` 走這條路徑，避免本機要裝 golang-migrate binary。

**Step 3**：驗證（暫時跳過，本 task 不要求 Postgres）

```bash
bash -n scripts/db/migrate.sh   # syntax check
```

**Step 4**：commit

```bash
chmod +x scripts/db/migrate.sh
git add migrations scripts/db
git commit -m "chore(db): set up golang-migrate with placeholder migration"
```

---

## Task 10：K8s `base/` 共通 manifest

**Files**:
- 砍掉舊 `ops/kubernetes/base/`，重建：
  - `ops/kubernetes/base/namespace.yaml`
  - `ops/kubernetes/base/configmap.yaml`
  - `ops/kubernetes/base/deployment-api.yaml`
  - `ops/kubernetes/base/deployment-worker.yaml`
  - `ops/kubernetes/base/deployment-scheduler.yaml`
  - `ops/kubernetes/base/deployment-web-employee.yaml`
  - `ops/kubernetes/base/deployment-web-merchant.yaml`
  - `ops/kubernetes/base/deployment-web-admin.yaml`
  - `ops/kubernetes/base/service-*.yaml`（5 個）
  - `ops/kubernetes/base/ingress.yaml`（4 hostnames）
  - `ops/kubernetes/base/hpa-api.yaml`
  - `ops/kubernetes/base/pdb-*.yaml`
  - `ops/kubernetes/base/networkpolicy-default-deny.yaml`
  - `ops/kubernetes/base/serviceaccount-api.yaml`
  - `ops/kubernetes/base/kustomization.yaml`

**範圍**：每個 manifest 最小可用版本（image 用 `tbite/api:dev` placeholder，env 預留 `DATABASE_URL` 等）。Container 安全：non-root、readOnlyRootFilesystem、drop ALL、seccompProfile RuntimeDefault。

**Step 1**：先 `git rm -rf ops/kubernetes/base ops/kubernetes/components ops/kubernetes/overlays`（舊內容沿用太重，全砍重來；observability 那塊不在這目錄）

**Step 2**：寫上述檔案

**Step 3**：驗證

```bash
kustomize build ops/kubernetes/base > /tmp/base.yaml
# 期望：無錯，YAML 解析通過
kubectl --dry-run=client apply -f /tmp/base.yaml 2>&1 | tail -5
```

**Step 4**：commit

```bash
git add ops/kubernetes/base
git commit -m "feat(ops): rewrite K8s base manifests for new architecture"
```

---

## Task 11：K8s `overlays/single-node`

**Files**：
- `ops/kubernetes/overlays/single-node/kustomization.yaml`
- `ops/kubernetes/overlays/single-node/postgres-ha.yaml`（最小：單 Postgres pod for dev；HA 留 P8）
- `ops/kubernetes/overlays/single-node/redis.yaml`
- `ops/kubernetes/overlays/single-node/nats.yaml`
- `ops/kubernetes/overlays/single-node/minio.yaml`
- `ops/kubernetes/overlays/single-node/nginx-ingress.yaml`（IngressClass）
- `ops/kubernetes/overlays/single-node/secrets-bootstrap.yaml`（明文 dev secrets，註明僅供 local）
- `ops/kubernetes/overlays/single-node/configmap-patch.yaml`

**範圍說明**：P0 不裝完整 HA，只起 single-pod Postgres / Redis / NATS / MinIO 讓本機跑得起來。完整 HA Helm chart 留 P8。

**Step 1**：寫 manifest

**Step 2**：驗證

```bash
kustomize build ops/kubernetes/overlays/single-node > /tmp/single-node.yaml
kubectl --dry-run=client apply -f /tmp/single-node.yaml
```

**Step 3**：commit

```bash
git add ops/kubernetes/overlays/single-node
git commit -m "feat(ops): add single-node overlay with self-hosted dependencies"
```

---

## Task 12：K8s `overlays/gcp`

**Files**：
- `ops/kubernetes/overlays/gcp/kustomization.yaml`
- `ops/kubernetes/overlays/gcp/cloudsql-binding.yaml`（ConfigMap + Service ExternalName 指 cloud-sql-proxy）
- `ops/kubernetes/overlays/gcp/memorystore-binding.yaml`
- `ops/kubernetes/overlays/gcp/gcs-binding.yaml`
- `ops/kubernetes/overlays/gcp/nats.yaml`（同 single-node, self-host on GKE）
- `ops/kubernetes/overlays/gcp/gke-ingress.yaml`（BackendConfig + ManagedCertificate）
- `ops/kubernetes/overlays/gcp/cloud-armor-policy.yaml`
- `ops/kubernetes/overlays/gcp/workload-identity.yaml`
- `ops/kubernetes/overlays/gcp/external-secrets.yaml`

**Step 1**：寫 manifest（多為 placeholder，標清楚 `# fill in PROJECT_ID / BUCKET / IP at deploy`）

**Step 2**：驗證

```bash
kustomize build ops/kubernetes/overlays/gcp > /tmp/gcp.yaml
kubectl --dry-run=client apply -f /tmp/gcp.yaml
```

**Step 3**：commit

```bash
git add ops/kubernetes/overlays/gcp
git commit -m "feat(ops): add gcp overlay binding to Cloud SQL/Memorystore/GCS"
```

---

## Task 13：根 `Makefile`

**Files**：
- Create：`Makefile`

**Step 1**：

```makefile
SHELL := /usr/bin/env bash

.PHONY: help dev-up dev-down dev-reset dev-app dev-logs \
        migrate-up migrate-down migrate-new \
        contract-sync test-go test-web test-e2e \
        render-overlay build clean

help:
	@awk -F':.*##' '/^[a-zA-Z_-]+:.*##/ {printf "  \033[36m%-20s\033[0m %s\n",$$1,$$2}' $(MAKEFILE_LIST)

dev-up: ## Start k3d cluster + apply single-node overlay
	@scripts/dev/dev-up.sh

dev-down: ## Tear down k3d cluster
	@scripts/dev/dev-down.sh

dev-reset: ## Tear down + delete volumes + reseed
	@scripts/dev/dev-reset.sh

dev-app: ## Run Go API + 3 SvelteKit dev servers locally
	@scripts/dev/dev-app.sh

dev-logs: ## Tail logs (service=name)
	@kubectl -n tbite logs -f -l app=$(service)

migrate-up: ## Apply pending migrations
	@scripts/db/migrate.sh up

migrate-down: ## Revert last migration
	@scripts/db/migrate.sh down 1

migrate-new: ## Create new migration (name=xxx)
	@scripts/db/migrate.sh create -ext sql -dir /migrations -seq $(name)

contract-sync: ## Generate OpenAPI from Go and regenerate TS client
	@echo "Placeholder for P1: contract-sync"

test-go: ## Go tests
	@cd services/api && go test ./...

test-web: ## Frontend checks
	@pnpm -r check && pnpm -r lint

render-overlay: ## Render kustomize overlay (env=single-node|gcp)
	@kustomize build ops/kubernetes/overlays/$(env)

build: ## Build all
	@pnpm -r build && cd services/api && go build -o /tmp/tbite ./cmd/tbite

clean:
	@rm -rf node_modules apps/*/node_modules packages/*/node_modules apps/*/build target
```

**Step 2**：建立 `scripts/dev/` 三個 stub script（先回 echo 訊息，實作放 Task 14）

**Step 3**：驗證

```bash
make help
```

期望：列出所有 target。

**Step 4**：commit

```bash
git add Makefile scripts/dev
git commit -m "chore: add root Makefile and dev script stubs"
```

---

## Task 14：`make dev-up` / `dev-app` 實作

**Files**：
- `scripts/dev/dev-up.sh`：建立 k3d cluster、import images、apply overlay
- `scripts/dev/dev-down.sh`：刪 cluster
- `scripts/dev/dev-reset.sh`：dev-down + 砍 volume + dev-up
- `scripts/dev/dev-app.sh`：local 起 Go API + 三個 pnpm dev 平行

**Step 1**：`dev-app.sh`（最先做，因為它不需 k3d，能驗證骨架）

```bash
#!/usr/bin/env bash
set -euo pipefail
trap 'kill 0' EXIT
( cd services/api && go run ./cmd/tbite --role=api ) &
pnpm --filter @tbite/employee dev &
pnpm --filter @tbite/merchant dev &
pnpm --filter @tbite/admin dev &
wait
```

**Step 2**：`dev-up.sh`（k3d 版）

```bash
#!/usr/bin/env bash
set -euo pipefail
CLUSTER=${CLUSTER:-tbite}
if ! k3d cluster list | grep -q "^${CLUSTER}\b"; then
  k3d cluster create "$CLUSTER" \
    --port "80:80@loadbalancer" --port "443:443@loadbalancer" \
    --k3s-arg "--disable=traefik@server:0"
fi
kubectl create namespace tbite --dry-run=client -o yaml | kubectl apply -f -
kustomize build ops/kubernetes/overlays/single-node | kubectl apply -f -
kubectl -n tbite rollout status deploy/api --timeout=120s || true
echo "Cluster up. Add these to /etc/hosts:"
echo "127.0.0.1 app.tbite.test merchant.tbite.test admin.tbite.test api.tbite.test"
```

**Step 3**：驗證 dev-app（不需 cluster）

```bash
make dev-app &
sleep 5
curl -fsS http://localhost:8080/healthz
curl -fsS http://localhost:5173/ | grep -q "P0 skeleton · employee"
curl -fsS http://localhost:5174/ | grep -q "P0 skeleton · merchant"
curl -fsS http://localhost:5175/ | grep -q "P0 skeleton · admin"
kill %1
```

期望：四個指令都成功。

**Step 4**：commit

```bash
git add scripts/dev
git commit -m "feat(dev): implement dev-up, dev-app, dev-down, dev-reset"
```

---

## Task 15：CI workflows 重寫

**Files**：
- 砍 `.github/workflows/postgresql-migration-foundation.yml`、`openapi-contract.yml`、`observability-slo-gate.yml`、`image-build.yml`、`e2e-smoke.yml`、`deploy.yml` 等舊版（部分 retain 後續 phase 改寫）
- Create：
  - `.github/workflows/ci-lint-test.yml`：go test + pnpm check + pnpm lint
  - `.github/workflows/ci-render-overlay.yml`：跑 `kustomize build` × 2 overlay
  - `.github/workflows/ci-build-images.yml`：build 4 images（api / web-employee / web-merchant / web-admin），P0 階段不 push registry

**範圍**：P0 只保留「程式碼能通過 lint + build + render」的 gate。complete contract / migration / load gate 留 P2-P8 個別重建。

**Step 1**：先 review 並砍掉不再適用的 workflow（看 `.github/workflows/`）

**Step 2**：寫三個新 workflow

**Step 3**：本地驗證 workflow YAML 語法（用 actionlint 或先靠 GH actions 跑）

```bash
# 如有 actionlint：
actionlint .github/workflows/*.yml
```

**Step 4**：commit

```bash
git add .github/workflows
git commit -m "ci: rewrite workflows for new monorepo skeleton"
```

---

## Task 16：頂層 `.dockerignore` / `.gitignore` / `README` 整理

**Files**:
- 更新 `.gitignore`（加 `target/`、`node_modules/`、`apps/*/build`、`apps/*/.svelte-kit`、`apps/*/dist`）
- 更新 `.dockerignore`
- 改寫 `README.md` 為新架構簡介
- 刪除 `docs/plans/2026-05-13-p0-deletion-manifest.md`（任務完成）

**Step 1**：寫檔

**Step 2**：commit

```bash
git add .gitignore .dockerignore README.md
git rm docs/plans/2026-05-13-p0-deletion-manifest.md
git commit -m "docs: rewrite README and clean up ignore lists for new skeleton"
```

---

## Task 17：P0 收尾驗證（exit criteria 全跑一遍）

**Step 1**：clean install

```bash
make clean
pnpm install
( cd services/api && go mod download && go build ./... )
```

**Step 2**：lint + test

```bash
make test-web
make test-go
```

**Step 3**：build all images（本地）

```bash
docker build -t tbite/api:dev      -f services/api/Dockerfile services/api
docker build -t tbite/web-emp:dev  -f apps/employee/Dockerfile .
docker build -t tbite/web-mer:dev  -f apps/merchant/Dockerfile .
docker build -t tbite/web-adm:dev  -f apps/admin/Dockerfile .
```

**Step 4**：overlay render

```bash
make render-overlay env=single-node | head -50
make render-overlay env=gcp | head -50
```

**Step 5**：local dev smoke

```bash
make dev-app &
sleep 8
for port in 8080 5173 5174 5175; do
  curl -fsS http://localhost:$port/healthz || curl -fsS http://localhost:$port/
done
kill %1 || true
```

**Step 6**：開 PR

```bash
git push -u origin feat/p0-skeleton
gh pr create --title "P0: monorepo skeleton + Go API + dual K8s overlay" \
  --body "$(cat <<'EOF'
## Summary
- 砍掉舊 Rust 後端與單一 SPA
- 建立 pnpm workspace：apps/{employee,merchant,admin} + packages/{tokens,ui,api-client}
- Go 模組化單體骨架 (cmd/tbite --role=api|worker|scheduler) + /healthz
- golang-migrate + placeholder migration
- K8s base + overlays/single-node + overlays/gcp（render-only，未實際 deploy）
- CI: lint/test, kustomize render, image build
- Makefile + dev scripts (make dev-up, dev-app, ...)

## Test plan
- [ ] `pnpm install` 成功
- [ ] `make test-web` 通過
- [ ] `make test-go` 通過
- [ ] `make render-overlay env=single-node` 通過
- [ ] `make render-overlay env=gcp` 通過
- [ ] `make dev-app` 起來後 4 個 health 端點都 200

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

---

## Exit Criteria（必須全綠才能 merge）

- [ ] `pnpm install` 在 clean checkout 上成功
- [ ] `pnpm -r check` 無錯
- [ ] `pnpm -r lint` 無錯
- [ ] `pnpm -r build` 三 app 都 build 成功
- [ ] `cd services/api && go build ./...` 成功
- [ ] `cd services/api && go test ./...` 通過（即使沒有測試也應 exit 0）
- [ ] `make render-overlay env=single-node` 輸出有效 YAML
- [ ] `make render-overlay env=gcp` 輸出有效 YAML
- [ ] `make dev-app` 啟動四個 process，curl http://localhost:8080/healthz 回 `{"status":"ok"}`，curl 三個前端 dev port 都回 200 且包含「P0 skeleton」字樣
- [ ] CI（lint-test + render-overlay + build-images）全綠

---

## 後續 Phase 預告

- **P1**：identity 模組 + OIDC（Google/GitHub）+ packages/api-client 改由 OpenAPI 生成 + employee 登入流
- **P2**：Postgres schema 第一波 + menu / quota / vendor + 員工瀏覽今日菜單 + Postgres-anchored 扣減
- **P3+**：見 [design doc §15](./2026-05-13-tbite-refactor-design.md)
